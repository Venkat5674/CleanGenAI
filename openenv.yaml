version: "1.0"
name: data-cleaning-env
description: AI agent for cleaning tabular data environment

models:
  - gpt-4o-mini
  - gpt-4-turbo

tasks:
  easy:
    grader: env.grader:grade
  medium:
    grader: env.grader:grade
  hard:
    grader: env.grader:grade

actions:
  - remove_duplicates
  - fill_nulls
  - convert_date
  - drop_column